// write a function that takes in an array of integers and returns the length of the longest peak in the array
    // A peak is defined as adjacent integers in the array that are strictly increasing until they 
    // reach a tip (the highest value in the peak).


// example input: 1, 3, 2, 1 
    // expected output 4

// a minimum of 3 integers is required to form a peak

export const getLongestPeak = (array) => {
    return 
}